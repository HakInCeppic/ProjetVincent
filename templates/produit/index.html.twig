{% extends 'base.html.twig' %}

{% block title %}Mes produit{% endblock %}

{% block body %}
{{form_start(formulaire)}}


<div class="case"><input type="text" name="name" class="name" placeholder="Name"><br>
	<div class="email">
        {{form_widget(formulaire.email)}}
    </div>

    <div class="subject">
        {{form_widget(formulaire.subject)}}
    </div>

    <div class="comment">
        {{form_widget(formulaire.comment)}}
    </div>
</div>

<button type="submit">envoyer</button>
{{form_end(formulaire)}}
{% endblock %}


